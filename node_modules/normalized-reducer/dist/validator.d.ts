import { ModelSchemaReader } from './schema';
import { Schema } from './interfaces';
export declare const validateSchema: (schema: Schema) => void;
export declare const cleanData: (data: any, schema: ModelSchemaReader, type: string) => {
    [key: string]: any;
};
